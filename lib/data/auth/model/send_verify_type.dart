/// To determine if you want to get the code to register or log in
enum SendVerifyType { LOGIN, REGISTER }

const SendVerifyTypeEnumMap = {
  SendVerifyType.LOGIN: 'LOGIN',
  SendVerifyType.REGISTER: 'REGISTER',
};
